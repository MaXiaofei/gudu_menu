-- ============================================================
-- V59: 标签字典改名——「下饭」→「下饭菜」（2026-08-18 用户定稿用词）
-- V58 合并目标词为「下饭」，用户偏好显示「下饭菜」，本迁移改名对齐。
-- 幂等：改名后「下饭」不再存在，重跑无操作。
-- ============================================================
UPDATE sys_dict SET name='下饭菜' WHERE dict_group='tag' AND name='下饭';
