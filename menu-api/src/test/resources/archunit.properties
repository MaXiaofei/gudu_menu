# ArchUnit 冻结规则（FreezingArchRule）配置
# 违规基线存放在 src/test/resources/archunit_store/（随代码提交，只减不增）
freeze.store.default.path=src/test/resources/archunit_store
# 首次运行（尚无基线文件）自动把当前违规记为基线，规则通过；之后只允许违规减少
freeze.store.default.allowStoreCreation=true
